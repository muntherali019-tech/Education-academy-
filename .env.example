# Copy to .env.local and fill in to enable homework photo marking in `npm run dev`.
# The key is read by the dev-server endpoint only — it never reaches the browser.
ANTHROPIC_API_KEY=

# Only needed when the marking endpoint is not served from /api/mark on this origin.
# VITE_MARKING_ENDPOINT=https://example.com/api/mark
